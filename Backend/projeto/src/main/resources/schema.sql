CREATE TABLE restaurante (
                             id INT PRIMARY KEY,
                             imagem VARCHAR(200),
                             nome VARCHAR(80),
                             local VARCHAR(40),
                             nota DECIMAL(2,1),
                             descricao VARCHAR(700)
);
